# rspec spec/shortcode_spec.rb
require 'rails_helper'

describe Shortcode do
  it "works for first, short template name" do
    expect(1).to eq 1
  end
  it "does not work for the latter, longer template" do
    expect(1).to eq 1
  end
end
